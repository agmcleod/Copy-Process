%h1= @element_type.name

%p Elements:
%ul
  - @element_type.elements.each do |ele|
    %li= ele.content
  
%p= link_to 'Delete this element type', site_element_type_path(@site, @element_type), method: :delete, confirm: 'Are you sure?'