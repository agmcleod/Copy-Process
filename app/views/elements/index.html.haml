%h1 Elements

- if @elements.size > 0
  %ul
    - @elements.each do |ele|
      %li= ele.content.html_safe
    
%p
  = link_to 'Site', site_path(@site)
  = link_to 'Element Types', site_element_types_path(@site)